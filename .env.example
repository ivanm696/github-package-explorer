# GitHub Personal Access Token (optional — increases API rate limit from 60 to 5000 req/h)
# Create at: https://github.com/settings/tokens/new
# Required scopes: public_repo (read-only)
VITE_GITHUB_TOKEN=
